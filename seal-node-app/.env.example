# Seal API Bridge Configuration

# Bearer token for authenticating requests to this bridge
# Generate a secure random token (e.g., using: openssl rand -hex 32)
BRIDGE_BEARER=your_bearer_token_here

# Seal Subscriptions API Token
# Get this from your Seal Subscriptions dashboard
SEAL_TOKEN=your_seal_api_token_here

# Server Port (defaults to 3000 if not set)
PORT=3000
